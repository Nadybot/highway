[package]
name = "highway"
version = "0.1.0"
authors = ["Jens Reidel <adrian@travitia.xyz>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
tokio-tungstenite = { version = "0.13", default-features = false, features = ["connect"] }
dashmap = "4.0"
env_logger = { version = "0.8", default-features = false, features = [
    "termcolor",
    "atty",
    "humantime"
] }
once_cell = "1.5"
leaky-bucket-lite = "0.1"
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
simd-json = { version = "0.3", default-features = false, features = [
    "allow-non-simd",
    "known-key",
    "neon",
    "serde_impl"
], optional = true }
tokio = { version = "1.1", default-features = false, features = [
    "rt-multi-thread",
    "macros"
] }
futures-util = "0.3"

[features]
simd = ["simd-json"]

[profile.release]
codegen-units = 1
debug = false
incremental = false
lto = true
opt-level = 3
panic = "abort"
